# Smart FBT Discount Function

This JavaScript Shopify Function is built explicitly through the extension package script.

From the app root, run:

```bash
yarn install
yarn dev
```

or build directly:

```bash
cd extensions/smart-fbt-discount
npm run build
```

The build command outputs `dist/index.wasm`.
